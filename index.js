
// Write your code here
// Given values to make the equations work
const num1 = 31;
const num2 = 2;
const num3 = 20;
const num4 = 16;

// Variable that multiplies num1 and num2 to get 62
const multiply = num1 * num2;  // 31 * 2 = 62

// Variable that generates a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Random integer between 1 and 100

// Variable that calculates the remainder of num3 divided by num4 (remainder is 4)
const mod = num3 % num4;  // 20 % 16 = 4

// Variable that finds the highest number in a set (max is 20)
const max = Math.max(10, 15, 20, 5);  // The highest number is 20
